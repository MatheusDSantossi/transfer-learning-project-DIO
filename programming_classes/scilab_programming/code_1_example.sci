side = input("Say the value of the side of one square: ");
area = side * side;

disp("The area of the square is: ");
disp(area);

// better code
side = input("Tell the value of one square side: ");
area = side * side;
printf("The area of the square is: %f", area);
